{
  "name": "google-drive-video-summarizer",
  "version": "1.0.0",
  "private": true,
  "description": "Google Drive Video Summarizer - Monorepo",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "start": "npm run start --workspace=web",
    "lint": "npm run lint --workspace=web"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
